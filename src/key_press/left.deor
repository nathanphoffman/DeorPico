macro key_left
	if cursor_col > 0
		cursor_col = cursor_col - 1
	else if cursor_row > 0
		# Consider restoring this if you want to use left tab to go up a row
		#cursor_row = cursor_row - 1
		#string new_line = lines at cursor_row
		#cursor_col = len(new_line)
